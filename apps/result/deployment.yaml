apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: result
  name: result
  namespace: app-result
spec:
  replicas: 1
  selector:
    matchLabels:
      app: result
  template:
    metadata:
      labels:
        app: result
    spec:
      containers:
      - image: gitopsdevacr.azurecr.io/result:1.0.0
        name: result
        ports:
        - containerPort: 80
          name: result
