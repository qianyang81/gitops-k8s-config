apiVersion: v1
kind: Service
metadata:
  labels:
    app: result
  name: result
  namespace: app-result
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: result
  type: LoadBalancer
status:
  loadBalancer: {}
