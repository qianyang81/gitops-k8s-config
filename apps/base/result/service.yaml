---
apiVersion: v1
kind: Service
metadata:
  name: result
  namespace: app-result
  labels:
    app: result
spec:
  selector:
    app: result
  ports:
    - name: http
      port: 80
      targetPort: 80
